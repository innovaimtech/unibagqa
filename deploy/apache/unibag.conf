<VirtualHost *:80>
    ServerName tudominio.com
    ServerAlias www.tudominio.com
    DocumentRoot /var/www/unibag/current/public

    <Directory /var/www/unibag/current/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/unibag_error.log
    CustomLog ${APACHE_LOG_DIR}/unibag_access.log combined
</VirtualHost>

<IfModule mod_ssl.c>
<VirtualHost *:443>
    ServerName tudominio.com
    ServerAlias www.tudominio.com
    DocumentRoot /var/www/unibag/current/public

    <Directory /var/www/unibag/current/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/tudominio.com/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/tudominio.com/privkey.pem

    ErrorLog ${APACHE_LOG_DIR}/unibag_ssl_error.log
    CustomLog ${APACHE_LOG_DIR}/unibag_ssl_access.log combined
</VirtualHost>
</IfModule>
